export type VideoListType = {
    videoID: string
    title: string
    date: string
}

export type MusicListType = {
    trackID: string
    platform: string
    intro: string
    title: string
    date: string
}